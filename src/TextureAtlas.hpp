#pragma once
#include <string>

const std::string TEXTURE_ATLAS_PATH =
    std::string(CMAKE_ROOT_DIR) + "/resources/textures/atlas.png";

constexpr int TEXTURE_SIZE = 16;  // Size of each texture in the atlas
